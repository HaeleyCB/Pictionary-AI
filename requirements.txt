Flask==2.2.2
gunicorn==20.1.0
transformers==4.24.0  # Specify a version (you can check the latest stable version)
torch==1.10.0  # Specify a version (you can check the latest stable version)
Pillow==8.4.0  # Specify a version (you can check the latest stable version)
